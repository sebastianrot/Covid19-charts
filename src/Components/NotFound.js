const NotFound = () => {
    return(
        <main style={{padding: '60px 0', display: 'flex', justifyContent: 'center', alignItems: 'center', flexDirection: 'column'}}>
            <span style={{fontSize: '40px', fontWeight: '600'}}>404</span>
            <span>Not found</span>
        </main>
    )
}

export default NotFound;